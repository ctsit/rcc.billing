% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/redcap_projects_history.R
\name{reconcile_redcap_projects_history_schema}
\alias{reconcile_redcap_projects_history_schema}
\title{reconcile_redcap_projects_history_schema}
\usage{
reconcile_redcap_projects_history_schema(
  source_conn,
  source_table,
  target_conn,
  target_table
)
}
\arguments{
\item{source_conn, }{a DBI connection to the database containing \code{source_table}}

\item{source_table, }{the name of the table to treat as the source of truth for column definitions}

\item{target_conn, }{a DBI connection to the database containing \code{target_table}}

\item{target_table, }{the name of the table to reconcile to match the source}
}
\value{
a dataframe of the drifted columns that were altered, as returned by
  \code{\link{get_column_type_drift}}
}
\description{
Detects columns shared between a source and target table whose type or nullability
has drifted, and alters the target table to match the source. Used to keep
\code{redcap_projects_history} in sync with upstream DDL revisions to REDCap's own
\code{redcap_projects} table (e.g. widened \code{VARCHAR}s or new \code{ENUM} values)
so that syncing data no longer fails with truncation errors.
}
\examples{
\dontrun{
rc_conn <- redcapcustodian::connect_to_redcap_db()
rcc_billing_conn <- connect_to_rcc_billing_db()

drift <- reconcile_redcap_projects_history_schema(
  source_conn = rc_conn,
  source_table = "redcap_projects",
  target_conn = rcc_billing_conn,
  target_table = "redcap_projects_history"
)
}
}
