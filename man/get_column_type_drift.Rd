% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/redcap_projects_history.R
\name{get_column_type_drift}
\alias{get_column_type_drift}
\title{get_column_type_drift}
\usage{
get_column_type_drift(source_conn, source_table, target_conn, target_table)
}
\arguments{
\item{source_conn, }{a DBI connection to the database containing \code{source_table}}

\item{source_table, }{the name of the table to treat as the source of truth for column definitions}

\item{target_conn, }{a DBI connection to the database containing \code{target_table}}

\item{target_table, }{the name of the table to check for drifted column definitions}
}
\value{
a dataframe with one row per drifted column: \code{column_name},
  \code{source_column_type}, \code{source_is_nullable}, \code{target_column_type},
  \code{target_is_nullable}
}
\description{
Compares the column definitions of a source table and a target table via
\code{information_schema.columns} and returns the columns that are present in both
tables but whose type or nullability differs. Used to detect when REDCap's own DDL
revisions to \code{redcap_projects} have drifted out from under the frozen
\code{redcap_projects_history} mirror table.
}
\examples{
\dontrun{
rc_conn <- redcapcustodian::connect_to_redcap_db()
rcc_billing_conn <- connect_to_rcc_billing_db()

drift <- get_column_type_drift(
  source_conn = rc_conn,
  source_table = "redcap_projects",
  target_conn = rcc_billing_conn,
  target_table = "redcap_projects_history"
)
}
}
